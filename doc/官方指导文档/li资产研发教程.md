\## 📃前置资料 ### 资产定义及分类 资产 = 组件 + 属性面板配置 <img src="https://cdn.nlark.com/yuque/0/2023/png/304578/1687853263523-c1ed03ca-c584-4abf-b642-ea4066348229.png" width="377" title="" crop="0,0,1,1" id="ud529c097" class="ne-image"> ### 资产研发说明 + 资产数量**不多**时，可以按下述[**第2章.组件资产**](#cAuMf)和[**第3章.图层资产**](#qn3X6)中说明的文件结构规范在本地项目文件中研发 + 资产数量**较多**时，可以继续放在本地项目，或者按照[**第4章.资产研发示例**](#kzWWR)中提供的项目框架将资产文件提取成独立包的方式来研发 ## 组件资产 ### 分类 #### 按研发角度分 - Type ![画板](https://cdn.nlark.com/yuque/0/2023/jpeg/196672/1685360293527-0193dc33-3026-49d4-bdab-ba0ddecb817d.jpeg) > **<font style="color:#2F4BDA;">研发同学</font>**开发组件时 > + **Layout - 布局组件** + **Container - 容器组件：容器内可随意添加原子资产**    - 容器组件：一般有侧边栏、工具栏、浮动面板容器 + **Atom - 原子组件：需要加载到容器的组件**    - 地图交互组件：自适应、切换城市组件、下载、位置查询    - 数据分析组件：筛选组件、统计组件、表格视图 + **Auto - 自加载组件：默认挂载到地图画布的组件**    - 地图交互组件：右键菜单    - 地图控件组件：放大缩小控件、底图切换控件、位置查询控件    - 图层交互组件：属性组件、提示框 tooltip  ```tsx /** * 组件资产类型： * Layout-布局组件 * Container-容器组件（容器内可随意添加原子资产） * Atom-原子组件（需要加载到容器的组件） * Auto-自加载组件（自动挂载到地图画布的组件） */ export type WidgetType = 'Layout' | 'Container' | 'Atom' | 'Auto'; ```  #### 按使用角度分 - Category > **<font style="color:#117CEE;">数据分析同学</font>**使用组件时 > + **Layout - 布局组件** + **Container - 容器组件**    - 侧边栏、工具栏、浮动面板容器 + **MapInteraction - 地图交互组件**    - 右键菜单 + **MapControl - 地图控件组件**    - 放大缩小控件、底图切换控件、位置查询控件 + **DataAnalysis - 数据分析组件**    - 筛选组件、统计组件、表格视图 + **LayerInteraction - 图层交互组件**    - 属性面板、提示框 tooltip + **string - 自定义分类**  ```tsx /** 组件资产分类 */ type WidgetCategory =  | 'Layout'  | 'Container'  | 'MapInteraction'  | 'MapControl'  | 'DataAnalysis'  | 'LayerInteraction'  | string; ```  ### 研发规范 组件资产的基础文件结构如下 ```bash ├─src │	├─ZoomControl │	│ ├─index.md // 组件资产描述文档，非必须 │	│ ├─index.tsx // 组件资产的入口文件 │	│ ├─Component.tsx   // 组件文件 │	│ ├─Component.less  // 组件样式文件，非必须 │	│ ├─registerForm.ts // 组件资产的属性面板配置 │	├─index.ts // 资产包的入口文件 ``` #### 资产包的入口文件 ```tsx import { ZoomControl } from './widgets'; const widgets = [ZoomControl]; export default {  version: 'v0.1',  layers: [],  widgets, }; ``` #### 资产的入口文件 ```tsx import { implementWidget } from '@antv/li-sdk'; import component from './Component'; import registerForm from './registerForm'; export default implementWidget({  version: 'v0.1',  metadata: {    name: 'ZoomControl',    displayName: '缩放器',    description: '地图层级缩放器',    type: 'Auto',    category: 'MapControl',  },  component,  registerForm, }); ``` #### 组件文件 ```tsx import React from 'react'; import type { ImplementWidgetProps } from '@antv/li-sdk'; import type { Properties } from './registerForm'; export interface ZoomControlProps extends ImplementWidgetProps, Properties {} const ZoomControl: React.FC<ZoomControlProps> = (props) => {  const { position } = props;   return (    <Zoom position={position} />  ); }; export default ZoomControl; ``` #### 资产的配置面板文件 ```typescript import type { PositionName } from '@antv/l7'; import type { WidgetRegisterForm, WidgetRegisterFormProps } from '@antv/li-sdk'; import { getWidgetSlotFormSchema } from '@antv/li-sdk'; /** * 属性面板生产的数据类型定义 */ export type Properties = {  /** 是否显示侧边栏面板 */  position?: PositionName; }; export default (props: WidgetRegisterFormProps): WidgetRegisterForm<Properties> => {  // 组件资产的配置表单面板 Schema，表单库 formily 的 Schema  const schema = {    position: {        title: '放置方位',        type: 'string',        'x-decorator': 'FormItem',        'x-component': 'Select',        'x-component-props': {          options: [            {              value: 'topleft',              label: '左上',            },            {              value: 'topright',              label: '右上',            },            {              value: 'bottomleft',              label: '左下',            },            {              value: 'bottomright',              label: '右下',            },          ],        },        default: 'bottomright',      },  };   return { schema }; };  ``` ### 🛠 研发实战：布局组件资产 #### step1：定义资产 ```tsx import { implementWidget } from '@antv/li-sdk'; import component from './Component'; import registerForm from './registerForm'; export default implementWidget({  version: 'v0.1',  metadata: {    name: 'MyLayout',    displayName: '布局组件',    description: '我的布局组件',    type: 'Layout',    category: 'Layout',  },  component, // Step 2 实现组件逻辑  registerForm, // Step 3 属性面板文件 }); ```  #### step2：实现组件逻辑 ```tsx import type { ImplementWidgetProps } from '@antv/li-sdk'; import { MapContainer } from '@antv/li-sdk'; import classNames from 'classnames'; import React, { useMemo } from 'react'; import type { Properties } from './registerForm'; import { CLS_PREFIX } from './constant'; import './Component.less'; export interface BaseLayoutProps extends Properties, ImplementWidgetProps<'content' | 'controls' | 'sidePanel'> {  children?: React.ReactNode; } const BaseLayout: React.FC<BaseLayoutProps> = (props) => {  const { showSidePanel, slotsElements, children } = props;   const mapContainerSlots = useMemo(    () => ({ content: slotsElements.content, controls: slotsElements.controls }),    [slotsElements.content, slotsElements.controls],  );   return (    <div className={CLS_PREFIX}>      <div className={`${CLS_PREFIX}__main`}>        <MapContainer          className={classNames(`${CLS_PREFIX}__map-container`)}          slotsElements={mapContainerSlots}        >          {children}        </MapContainer>        {showSidePanel && (          <div className={`${CLS_PREFIX}__side-panel`}>            {slotsElements.sidePanel ? slotsElements.sidePanel({}) : null}          </div>        )}      </div>    </div>  ); }; export default BaseLayout; ```  #### step3：定义属性面板文件 ```typescript import type { WidgetRegisterForm, WidgetRegisterFormProps } from '@antv/li-sdk'; import { getWidgetSlotFormSchema } from '@antv/li-sdk'; /** * 属性面板生产的数据类型定义 */ export type Properties = {  /** 是否显示侧边栏面板 */  showSidePanel: boolean; }; export default (props: WidgetRegisterFormProps): WidgetRegisterForm<Properties> => {  // 属性面板表单的 Schema 定义，来自表单库 formily 的 Schema  const schema = {    ...getWidgetSlotFormSchema(props, 'sidePanel', '侧边栏'),    showSidePanel: {      title: '显示侧边栏',      type: 'boolean',      'x-decorator': 'FormItem',      'x-component': 'Switch',      default: true,    },  };   return { schema }; }; ```  ## 图层资产 ### 研发规范 与组件资产类似，研发一个图层资产有以下几个基本文件 ```bash ├─src │	├─BubbleLayer │	│ ├─index.md // 图层资产描述文档，非必须 │	│ ├─index.tsx // 图层资产的入口文件 │	│ ├─Component.tsx   // 图层组件文件 │	│ ├─Component.less  // 图层组件样式文件，非必须 │	│ ├─registerForm.ts // 图层资产的属性面板配置 │	├─index.ts // 资产包的入口文件 ``` #### 资产包的入口文件 ```tsx import BubbleLayer from './BubbleLayer'; const layers = [BubbleLayer]; export default {  version: 'v0.1',  layers,  widgets: [], }; ``` #### 资产的入口文件 ```tsx import { implementLayer } from '@antv/li-sdk'; import React from 'react'; import component from './Component'; import registerForm from './register-form'; const ICON = () => {  return (    <svg viewBox="0 0 64 64" width="56px" height="56px" style={{ fill: 'currentcolor' }}>      <circle cx="29.4" cy="31.6" r="8.4" className="cr1" />      <circle cx="48.5" cy="15.7" r="6.5" className="cr2" />      <circle cx="11" cy="44.2" r="3" className="cr3" />      <circle cx="50" cy="44.2" r="5" className="cr4" />      <circle cx="34" cy="54.2" r="3" className="cr5" />      <circle cx="14" cy="16.2" r="4" className="cr6" />    </svg>  ); }; export default implementLayer({  version: 'v0.1',  metadata: {    name: 'BubbleLayer',    displayName: '气泡图层',    description: '用于点数据以气泡形式展示，支持描边、文本标注、多选等功能',    type: 'Layer',    icon: ICON,    color: 'orange',  },  defaultVisConfig: {    zIndex: 0,    radius: 10,    fillColor: 'rgb(90, 216, 166)',    opacity: 0.8,    strokeColor: 'rgb(146, 112, 202)',    lineWidth: 0,    lineOpacity: 1,    label: {      style: {        fill: '#c0c0c0',        fontSize: 15,        textAnchor: 'center',        textOffset: [0, 0],      },    },    minZoom: 0,    maxZoom: 24,    blend: 'normal' as const,  },  component,  registerForm, }); ``` #### 组件文件 ```tsx import type { BubbleLayerProps } from '@antv/larkmap'; import { BubbleLayer } from '@antv/larkmap'; import type { ImplementLayerProps } from '@antv/li-sdk'; import React from 'react'; export interface BubbleLayerWrapperProps extends BubbleLayerProps, ImplementLayerProps {} const BubbleLayerWrapper: React.FC<BubbleLayerWrapperProps> = (props) => {  return <BubbleLayer {...props} />; }; export default BubbleLayerWrapper; ``` #### 资产的配置面板文件 ```typescript import type { BubbleLayerStyleAttributeValue } from '@antv/larkmap'; import { bubbleLayerStyleConfigToFlat, bubbleLayerStyleFlatToConfig } from '@antv/larkmap'; import type { LayerRegisterForm, LayerRegisterFormProps, LayerRegisterFormResultType } from '@antv/li-sdk'; import getSchema from './schema'; /** * 表单数据格式转换，将结构化数据转换为表单的平铺结构 */ const toValues = (config: LayerRegisterFormResultType<BubbleLayerStyleAttributeValue>) => {  const { sourceConfig, visConfig } = config;  const { parser } = sourceConfig;  const coordinateType = sourceConfig.parser?.geometry ? 'geometry' : 'table';  const pointCoordinate = parser.geometry    ? { geometry: parser.geometry }    : { longitude: parser?.x, latitude: parser?.y };   return {    coordinateType,    ...pointCoordinate,    ...bubbleLayerStyleConfigToFlat(visConfig),  }; }; /** * 表单数据格式转换，将表单的平铺数据结构转为结构化数据 */ const fromValues = (values: Record<string, any>): LayerRegisterFormResultType<BubbleLayerStyleAttributeValue> => {  const pointCoordinate = values.geometry ? { geometry: values.geometry } : { x: values.longitude, y: values.latitude };  const sourceConfig = {    parser: {      type: 'json',      ...pointCoordinate,    },  };  const visConfig = bubbleLayerStyleFlatToConfig(values);   return {    sourceConfig,    visConfig: {      ...visConfig,    },  }; }; export default (props: LayerRegisterFormProps): LayerRegisterForm<BubbleLayerStyleAttributeValue> => {  // 属性面板表单的 Schema 定义，来自表单库 formily 的 Schema  const schema = getSchema(props.datasetFields);  return {    schema,    toValues,    fromValues,  }; }; ```  ## 🖥 资产研发示例包 + [资产研发示例包 li-template-assets](https://github.com/antvis/L7VP/tree/master/examples/li-template-assets)：资产包框架  + 组件空文件夹 + [资产研发分析包 li-analysis-assets](https://github.com/antvis/L7VP/tree/master/packages/li-analysis-assets)：资产包框架 + 分析场景组件 